import trailing
#print(trailing.trailing_zeros_fact(10000))

#when calling trailing_zeros_fact() from trailing, if name == main block in trailing doesn't run.

print(trailing.trailing_zeros_fact(500))